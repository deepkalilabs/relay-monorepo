Review only the increment from {{DEFAULT_BRANCH}} to HEAD for:
{{GOAL}}

Read {{PLAN_FILE}} and {{PROGRESS_FILE}}, then run:
- `git log {{DEFAULT_BRANCH}}..HEAD --oneline`
- `git diff {{DEFAULT_BRANCH}}...HEAD`

Launch all five agents in one parallel batch and wait for all results:
{{agent:quality}}
{{agent:implementation}}
{{agent:testing}}
{{agent:simplification}}
{{agent:documentation}}

Verify every reported finding against the changed code. Discard false positives,
duplicates, and issues that are pre-existing and unaffected by this increment.
Fix every confirmed issue caused by the increment, run only the focused checks
required by the plan and AGENTS.md, and commit the fixes.

If this iteration found and fixed anything, summarize the fixes without emitting
a marker so the review loop runs again. If it found no confirmed issue, end with:
<<<RALPHEX:REVIEW_DONE>>>

If a confirmed issue cannot be fixed, end with:
<<<RALPHEX:TASK_FAILED>>>
