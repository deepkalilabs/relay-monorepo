Perform the final critical/major review of the increment from
{{DEFAULT_BRANCH}} to HEAD for {{GOAL}}.

Read {{PLAN_FILE}} and {{PROGRESS_FILE}} and inspect:
`git diff {{DEFAULT_BRANCH}}...HEAD`

Launch these agents together and wait for both:
{{agent:quality}}
{{agent:implementation}}

Verify findings against the changed code. Ignore style, minor, unrelated, and
pre-existing issues. Fix and commit only confirmed critical or major regressions
introduced by this increment, using the focused checks required by AGENTS.md.

If fixes were committed, summarize them without a marker so review repeats. If
there are no confirmed critical or major issues, end with:
<<<RALPHEX:REVIEW_DONE>>>

If a confirmed issue cannot be fixed, end with:
<<<RALPHEX:TASK_FAILED>>>
