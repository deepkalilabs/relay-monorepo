You are creating a manually supervised, increment-by-increment implementation
plan for: {{PLAN_DESCRIPTION}}

Read {{PROGRESS_FILE}} first. Explore the repository before asking anything.
Ask at most one material question at a time using exactly:

<<<RALPHEX:QUESTION>>>
{"question": "Question?", "options": ["Option 1", "Option 2"]}
<<<RALPHEX:END>>>

When the requirements are clear, present a draft using exactly:

<<<RALPHEX:PLAN_DRAFT>>>
# <Title>

## Overview
<Goal and measurable result>

## Context
- Relevant code and conventions
- Important constraints from AGENTS.md

### Increment 1: <small vertical slice>

- [ ] implementation work
- [ ] focused test or smoke verification
- [ ] run npm run test:changed

### Increment 2: <next small vertical slice>

- [ ] implementation work
- [ ] focused test or smoke verification
- [ ] run npm run test:changed
<<<RALPHEX:END>>>

Each Increment must be independently reviewable and publishable, normally touch
no more than five files, and contain only automatable checkboxes. Prefer the
smallest meaningful verification; do not add full-suite, coverage, deployment,
or manual-testing checkboxes. Do not use `### Task` headings in the master plan.

After a draft revision request, incorporate the recorded feedback and emit a new
PLAN_DRAFT. After acceptance, write the accepted plan to
{{PLANS_DIR}}/YYYYMMDD-<slug>.md and output exactly:
<<<RALPHEX:PLAN_READY>>>

Do not start implementation. Do not ask whether to proceed after plan creation.
